name: Linux build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build aura.so
      working-directory: binary/linux
      run: make
    #- name: make check
      #run: make check
    #- name: make distcheck
      #run: make distcheck
